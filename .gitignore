# Pasta de imagens-fonte (PNGs originais). As versões otimizadas
# já estão em assets/web/. Mantida apenas localmente.
projects-img/

# Outras pastas de trabalho não usadas pelo site publicado
uploads/
screenshots/

# Configuração local do Claude Code (varia por máquina)
.claude/settings.local.json

# Sistema
Thumbs.db
.DS_Store
